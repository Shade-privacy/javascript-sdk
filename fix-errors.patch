--- a/src/index.ts
+++ b/src/index.ts
@@ -185,7 +185,8 @@ if (import.meta.url === `file://${process.argv[1]}`) {
       try {
         await sdk.initialize();
         console.log('✅ SDK initialized');
-      } catch (err) {
+      } catch (err: any) {
+        const error = err as Error;
         console.log('⚠️  Services not available (expected):', error.message);
         console.log('💡 Start services with: npm run services:all');
       }
@@ -197,7 +198,8 @@ if (import.meta.url === `file://${process.argv[1]}`) {
       console.log('4. Execute private transactions');
       
     } catch (error) {
-      console.error('❌ Error:', error.message);
+      const err = error as Error;
+      console.error('❌ Error:', err.message);
     }
   }
   
